<div class="container">

	<p>
	  <strong>Name:</strong>
	  <%= @course.name %>
	</p>

	<p>
	  <strong>Url:</strong>
	  <%= @course.url %>
	</p>

	<%= link_to 'Edit', edit_course_path(@course) %> |
	<%= link_to 'Back', courses_path %>
</div>